# SRT Gateway Agent Context

Active project: `C:\worklocal\ikoSRTgateway`

Shared agent roles: `C:\worklocal\ikodevteam\.agents\roles`

## Product

SRT Gateway is a web-managed stream routing gateway for broadcast-style SRT, UDP, RTMP, RIST, and HLS workflows. It provides a browser UI/API, stores desired service configuration in Postgres/Redis, runs FFmpeg pipelines on worker containers, publishes telemetry, and supports redundant worker ownership.

Current deployment target:

- Physical single server with two worker instances.
- API/UI: `10.75.51.40:8000`.
- Grafana: `10.75.15.3:4000` and `10.75.51.40:4000`.
- `worker-primary` video main: `10.70.15.3`.
- `worker-backup` video backup: `10.71.15.3`.
- Redis/Postgres are Docker-internal for single-server deployment unless a multi-server HA topology requires management-network exposure.
- Multi-server HA requires Redis/Postgres reachable on the management network and shared preview/HLS storage mounted on every control-plane and worker host.
- Access is through the DMZ; API/UI and Grafana should bind only to management IPs.
- Video input/output binding should be operator-selectable.

## Current Direction

- Production should use a separate explicit production compose file, not overwrite the server-local `docker-compose-microservices.yml`.
- `docker-compose.production.yml` is the canonical production compose file for both single-server HA and multi-server HA, selected through profiles and env files.
- Older compose files are lab/legacy compatibility paths and should not be presented as the production HA contract.
- Production workers use explicit `primary` and `backup` roles.
- Legacy `worker_1` and `local_bind_ip` remain compatibility/lab paths, but should not be the production UI contract.
- Production services should use `node_bindings.primary` and `node_bindings.backup` with separate `input_bind_ip` and `output_bind_ip`.
- Interface binding now uses UI dropdowns backed by configured hardware/interface inventory from `/api/interfaces`.
- Automatic failback remains out of scope until guarded operationally.

## Route Editor V2 Direction

Engineering feedback in `docs/RE_ SRT GW for testing - Adir Hadad - Outlook.pdf` should be treated as product input for the next route-configuration slice.
The full product scope map is captured in `docs/route-editor-v2-product-scope-map.md`.

- Defer SRT `Rendezvous`; current planning should cover SRT `Listener` and `Caller` only.
- Route creation/editing should become protocol-aware instead of exposing one flat set of fields for every route.
- Source protocols should expose only relevant fields for the selected protocol.
- UDP input/output should distinguish `Unicast` and `Multicast`.
- SRT input/output should distinguish `Listener` and `Caller` behavior.
- Route forms should separate network/link parameters from protocol-specific parameters.
- Operators should be able to select network interface/address/port for each relevant source and destination path.
- Path redundancy should be explicit in the UI and data model, adding a secondary interface/address/port row when enabled.
- SRT Stream ID should support a structured default builder and a raw custom Stream ID.
- Destination SRT Stream ID behavior should match input Stream ID behavior.
- A future Network menu may manage NIC IP settings via Linux Netplan, but write-capable Netplan changes are a separate high-risk feature and should not be mixed into Route Editor V2 without a dedicated safety design.

Architectural implementation direction:

- Add Route Editor V2 as an additive structured contract while keeping the existing flat service fields compatible.
- Store new protocol-specific configuration under structured `source` and `destinations` objects.
- During transition, derive legacy fields such as `source_protocol`, `source_mode`, `source_ip`, `source_port`, `destination_url`, `streamid`, `latency_ms`, and `passphrase` from the structured contract where needed.
- Keep `node_bindings` for worker-role input/output binding; do not use it as the path redundancy model.
- Add reusable endpoint, link-parameter, SRT-parameter, and Stream ID models in the API schema before changing worker behavior.
- Update FFmpeg command construction only after schema compatibility and UI serialization tests are in place.
- Architect resolved Dev Lead handoff gaps in `docs/route-editor-v2-implementer-handoff.md`: `destinations` list default factory, multi-destination behavior, Stream ID derivation template, and normalization helper location.
- Dev Lead handover for the updated plan is captured in `docs/route-editor-v2-dev-lead-handover.md`.
- Dev Lead approved backlog item 8 for implementation and created `docs/route-editor-v2-dev-lead-implementer-instructions.md`.
- Implementer should persist structured `source` and `destinations` as JSON columns and normalize through `api/route_normalizer.py`.
- Route Editor V2 Structured Contract is implemented:
  - `api/schemas.py` accepts structured `source` and `destinations` while preserving flat fields.
  - `api/route_normalizer.py` derives compatibility fields from structured input and builds structured objects from flat payloads.
  - `api/models.py` stores `source` and `destinations` as JSON.
  - `api/main.py` adds PostgreSQL bootstrap columns and normalizes create/update payloads before persistence and Redis sync.
  - Enabled structured destinations must have a usable URL or primary endpoint before they can satisfy the normal-output requirement.
  - Enabled path redundancy requires `mode: manual` and a secondary endpoint with a port; runtime failover behavior remains out of scope for this slice.
  - `tests/test_route_editor_v2_contract.py` covers structured contract validation, derivation, rejection cases, API round-trip, and Redis sync.

## HLS Decisions

HLS output is a local generated output mode, not a normal `destination_url` protocol.

- If no HLS output is enabled, `destination_url` is required.
- If low-res or full HLS output is enabled, `destination_url` may be empty.
- A service may have both normal destination output and HLS output.
- Low-res HLS generates `360p` and `480p` with a short rolling buffer, currently 10 seconds.
- Full HLS generates `720p` and `1080p` with configurable buffer up to 24 hours.
- Full HLS is resource intensive and requires disk-based buffer handling.
- HLS source is supported as `source_protocol: hls` with `source_url`.
- Initial HLS source implementation may ingest and regenerate outputs; smart passthrough/cache is future work.

## Current Implementation State

Recent implementation changes added:

- `source_url` and `source_protocol: hls`.
- `hls_outputs` with low/full profiles.
- Conditional `destination_url` validation.
- Low/full HLS FFmpeg output branches.
- UDP destination builder.
- UI controls for HLS source and low/full HLS output.
- Interface inventory endpoint and primary/backup input/output binding dropdowns.
- Full HLS service limit, disk free-space checks, optional storage quota checks, and cleanup of generated HLS artifacts on stop.
- Full HLS service limits count enabled/running-intended services only; disabled service templates do not consume capacity until they are started or enabled.
- `/api/services/{id}/start` validates Full HLS capacity before turning a disabled template into a running-intended service.
- HLS master playlists without hard-coded 16:9 `RESOLUTION` metadata.
- Legacy `StreamManager` passes its `node_role` into FFmpeg command building.
- Wider responsive create/edit modal sizing.
- Production compose uses disk-backed `shared_previews` storage and explicit HLS guardrail environment defaults.
- Production compose supports env-selectable `PREVIEW_STORAGE`; multi-server deployments must set it to a real shared filesystem path.
- Production env examples now cover single-server, control-plane, primary worker, and backup worker layouts.
- Tests for API validation, FFmpeg command construction, frontend static expectations, worker routing, and legacy stream manager binding behavior.

Latest test command:

```powershell
docker compose -f docker-compose-microservices.yml exec -T api python -m pytest
```

Last known result after rebuilding API image and installing `requirements-test.txt` in the running API container: `73 passed` with no pytest warning summary.

Latest production compose validation rendered successfully for:

- `.env.production.single-server.example`
- `.env.production.control-plane.example`
- `.env.production.primary.example`
- `.env.production.backup.example`

## QA Handoff State

Backlog items 1 through 5 are implemented and ready for QA review:

- Binding UI now hides visible `worker_1` production controls and uses primary/backup interface dropdowns.
- Full HLS has bounded duration, configurable service count, disk checks, storage quota checks, and stop cleanup.
- HLS master playlists no longer advertise hard-coded resolutions.
- Legacy stream manager output binding behavior is aligned with worker role behavior.
- Create/edit modal is wider, scrollable, and mobile responsive.

## Repository Notes

- Do not overwrite server-local Docker compose changes without explicit approval.
- Treat uncommitted files as user/agent work. Do not revert unrelated changes.
- Prefer `docker compose` verification because host Python may not be available.
- Update this file when product direction, architecture, or deployment assumptions change.
